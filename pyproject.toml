[project]
name = "rag-researcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "langchain",
    "langchain-ollama",
    "langchain-chroma",
    "pypdf",
    "langchain-community",
    "chromadb",
]
